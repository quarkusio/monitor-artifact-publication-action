name: 'Monitor artifact publication'
description: 'Monitor artifact publication to Maven Central'
inputs:
  github-token:
    description: 'GitHub token'
    required: false
runs:
  using: 'docker'
  image: 'docker://ghcr.io/quarkusio/monitor-artifact-publication-action:main'